[package]
name = "mps-formula"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
mps-bindgen-macro = { path = "../mps-bindgen-macro" }

[features]
default = []
