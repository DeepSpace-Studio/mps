[package]
name = "mps-web"
version.workspace = true
edition.workspace = true

[dependencies]
dioxus = { version = "0.6", features = ["fullstack", "router"] }
dioxus-i18n = { version = "0.5", default-features = false }
fluent = "0.16"
unic-langid = "0.9"

[features]
default = []
