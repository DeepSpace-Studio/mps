// Module-mirror directory for `mps_formula::rotor` — mirrors the layout
// used by `spaceflight/` (`mod.rs` declares the per-domain submodules).
//! `rotor` test mirror — split per-domain like the formula side.

pub mod blade_element;
pub mod momentum;
pub mod performance;
pub mod vortex;
