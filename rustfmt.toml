# rustfmt.toml — formatting configuration for mps_rigid_body
# Only stable-channel options are used.

edition = "2024"
max_width = 100
tab_spaces = 4
hard_tabs = false
reorder_imports = true
newline_style = "Auto"
remove_nested_parens = true
